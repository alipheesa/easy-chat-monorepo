import { forwardRef } from "react";

export const PopupMenuDivider = forwardRef(function PopupMenuDivider() {
  return (
    <div
      className="w-[95%] border-t-[1px] border-gray-400 opacity-30 mt-0.5 mb-1 self-center 
    rounded-full"
    />
  );
});
